<p id="notice"><%= notice %></p>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Project</th>
    <th>Task</th>
    <th>Date</th>
    <th>Hours done</th>
    <th>Description</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @entry.project.name %></td>
        <td><%= @entry.task.name %></td>
        <td><%= @entry.date_when_done %></td>
        <td><%= @entry.hours_done %></td>
        <td><%= @entry.description %></td>
        <td><%= link_to 'Edit', edit_entry_path(@entry), class:"btn btn-primary" %></td>
        <td><%= link_to 'Destroy', @entry, method: :delete, class:"btn btn-primary", data: { confirm: 'Are you sure?' } %></td>
      </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_entry_path(@entry), class:"btn btn-primary" %> |
<%= link_to 'Back', entries_path, class:"btn btn-primary" %>
